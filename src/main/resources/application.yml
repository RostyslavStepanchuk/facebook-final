server:
  port: 8080

spring:
  profiles:
    active: dev

  security:
      jwt-secret:
        ${random.uuid}

  mail:
    host: smtp.gmail.com
    port: 587
    username: finalfacebook00@gmail.com
    password: cjvbyzofwjrjbzwk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

---

spring:
  profiles: prod
  datasource:
    url: jdbc://mysql://localhost:3306/mydb
    username: user
    password:
  jpa:
    hibernate:
      ddl-auto: none
